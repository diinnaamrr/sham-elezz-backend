# Resolver for Docker DNS (helps avoid 502 when app starts after nginx)
resolver 127.0.0.11 valid=10s ipv6=off;

# HTTP server (keep working, no redirect yet)
server {
    listen 80;
    listen [::]:80;

    server_name sham-el-ezz.com www.sham-el-ezz.com _;
    root /var/www/html/public;
    index index.php index.html;

    charset utf-8;

    # صور وملفات مرفوعة – مسار /storage/app/public/ (يُستخدم في بعض الـ views)
    location ^~ /storage/app/public/ {
        alias /var/www/html/storage/app/public/;
        expires 30d;
        access_log off;
        log_not_found off;
    }

    # ملفات الـ storage (روابط Laravel storage:link → /storage/xxx)
    location ^~ /storage/ {
        alias /var/www/html/storage/app/public/;
        expires 30d;
        access_log off;
        log_not_found off;
    }

    # صور وملفات ثابتة تحت /public/assets/ (landing, admin) – أولوية أعلى
    location ^~ /public/assets/ {
        root /var/www/html;
        try_files $uri =404;
        expires 30d;
        access_log off;
        log_not_found off;
        add_header Cache-Control "public, immutable";
    }

    # CSS, JS, صور تحت /public/ – امتدادات معروفة
    location ~* ^/public/.*\.(css|js|png|jpg|jpeg|gif|ico|svg|woff2?|eot|ttf|otf|mp4|webm|webp|json|xml)$ {
        root /var/www/html;
        try_files $uri =404;
        access_log off;
        log_not_found off;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # باقي ملفات /public/ (مجلدات أو PHP)
    location ^~ /public/ {
        root /var/www/html;
        try_files $uri $uri/ =404;
        access_log off;
        log_not_found off;
        expires 30d;
        location ~ \.php$ {
            include fastcgi_params;
            fastcgi_pass app:9000;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        }
    }

    # Default Laravel front controller
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log;

    sendfile off;

    client_max_body_size 100M;

    location ~ \.php$ {
        try_files $uri =404;
        set $upstream_app app;
        fastcgi_pass $upstream_app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 300;
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
        fastcgi_busy_buffers_size 256k;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# HTTPS server – تفعيله بعد وضع الشهادات في docker/ngnix/ssl/ (cert.pem و key.pem)
# Uncomment when cert.pem and key.pem exist in docker/ngnix/ssl/
#
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     http2 on;
#     server_name sham-el-ezz.com www.sham-el-ezz.com;
#
#     root /var/www/html/public;
#     index index.php index.html;
#     charset utf-8;
#
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     location ^~ /storage/ {
#         alias /var/www/html/storage/app/public/;
#         try_files $uri =404;
#         access_log off; log_not_found off; expires 30d;
#     }
#     location ^~ /public/ {
#         alias /var/www/html/public/;
#         try_files $uri $uri/ =404;
#         access_log off; expires 30d;
#         location ~ \.php$ {
#             include fastcgi_params;
#             fastcgi_pass app:9000;
#             fastcgi_index index.php;
#             fastcgi_param SCRIPT_FILENAME $request_filename;
#         }
#     }
#     location / { try_files $uri $uri/ /index.php?$query_string; }
#     location = /favicon.ico { access_log off; log_not_found off; }
#     location = /robots.txt  { access_log off; log_not_found off; }
#     access_log /var/log/nginx/access.log;
#     error_log  /var/log/nginx/error.log;
#     sendfile off;
#     client_max_body_size 100M;
#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_pass app:9000;
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         include fastcgi_params;
#         fastcgi_param HTTP_X_FORWARDED_PROTO https;
#         fastcgi_param HTTPS on;
#         fastcgi_read_timeout 300;
#         fastcgi_buffer_size 128k;
#         fastcgi_buffers 4 256k;
#         fastcgi_busy_buffers_size 256k;
#     }
#     location ~ /\.(?!well-known).* { deny all; }
# }

